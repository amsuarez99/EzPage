page MyFirstMFPage

string hola = "hola"
int abcd = 12
int something = 1234

string somethingElse(int hola, int hola2) {
  int mmmmm = abcd / (something + hola)
  int myVar
  while(myVar < 70) {
    myVar = myVar + 1
  }
  for(myVar = 100 to 200) {
    something = something / something
  }
  return "hello world"
}

int secondFunction(int someVar) {
  if(somethingElse(someVar, 23) == "hello world") {
    return 1
  }
  return 2
}

void render() {
  container() {
    heading(size: "h1", text: "hello 1")
    heading(size: "h2", text: "hello 2")
  }
}