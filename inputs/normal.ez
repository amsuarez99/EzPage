page MyFirstMFPage

string hola = "something"
int abcd = 12
int something = 1234

string somethingElse(int hola, int hola2) {
  int result = abcd / something + hola
  string hello = "hello"
  return hello
}

void render() {
  container() {
    // renderFnParam dir, -1, "size" (constante string)
    // renderFnParam dir, -1, "text" (constante string)
    heading(size: "h1", text: "hello 1")
    heading(size: "h2", text: "hello 2")
  }
}